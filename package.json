{
  "name": "prompter",
  "version": "0.1.0",
  "private": true,
  "description": "Prompter - a small local UI + API for managing LLM instructions",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "vite": "^7.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.4",
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9"
  }
}
