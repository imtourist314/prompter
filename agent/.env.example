# Database connection URL used by AgentAPI
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/corp

# Poll interval override for AgentDaemon (seconds)
AGENT_DAEMON_REFRESH=60

# Directory where AgentDaemon stores its config
AGENT_DAEMON_HOME=~/.agent_daemon
